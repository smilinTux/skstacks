# hello_pack — example skwire pack

A complete, copy-pasteable pack. Copy this directory, rename `hello` → your
project, swap the nodes/injector for yours, and `pip install` it — skwire will
discover and configure it conversationally.
