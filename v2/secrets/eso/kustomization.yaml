apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# ESO wiring for the SKStacks secret plane. The ClusterSecretStore is the
# cluster-wide bridge to the chosen backend; ExternalSecrets are per-workload.
resources:
  - clustersecretstore-openbao.yaml
  - externalsecret-example.yaml
