INSTANCENAME=skorch
CLUSTERNAME={{ skorch.CLUSTERNAME | default(cluster_name) }}
DOMAIN={{ skorch.DOMAIN | default(domain) }}
INSTANCE={{ skorch.INSTANCE | default('primary') }}
APP_ENV={{ skorch.APP_ENV | default('prod') }}
